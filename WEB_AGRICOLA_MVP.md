# 農家樂線上桌 MVP 規格

本文件整理「BGA 風格 2D 線上農家樂」的第一版方向。目標不是複製 TTS 的 3D 桌面，而是做出固定玩家群可以直接取代 TTS 的網頁版農家樂。

參考來源：

- BGA 農家樂介面截圖：主板點擊式行動、右側玩家摘要、右側 log、玩家農場板、卡牌區。
- 目前 TTS 農家樂存檔：`TS_AutoSave_3.json`，已有補料、派人、返家、資源計數、卡牌整理等半自動流程。
- 本機錄影：`C:\Users\User\Videos\Captures\Tabletop Simulator 2026-06-16 01-51-30.mp4`，確認目前 TTS 使用方式與痛點。

## 產品定位

做一個「農家樂專用線上桌」，給固定玩家群用瀏覽器完成一局 4 人農家樂。

核心原則：

- 主板流程參考 BGA：點行動格，不做 3D 拖拉。
- 卡牌體驗參考爐石 / 遊戲王：底部手牌、hover 放大、已出牌區、卡片搜尋。
- 第一版卡牌效果半自動：可以打出、標記、手動發動，不追求全卡牌自動連動。
- 操作紀錄必須完整，因為半自動卡牌效果需要 log 來追蹤。
- 優先讓熟手玩家玩得快，不把第一版做成教學平台。

## 第一版目標

第一版成功標準：

- 4 位玩家可以進同一個房間。
- 玩家可以選顏色，系統開始時自動隨機分配座位。
- 支援輪抽設定：同時輪抽或輪流輪抽。
- 輪流輪抽時，職業卡順時鐘傳，次要發展卡逆時鐘傳。
- 可以完成 14 回合基本流程。
- 可以補料、派家庭成員、拿資源、返家、收成提示。
- 可以管理手牌、打出職業 / 次要發展卡 / 主要發展卡。
- 卡牌效果可以用手動快捷鍵發動並寫入 log。
- 中離後回到同一房間能看到目前局面。

第一版不追求：

- 全卡牌效果自動判斷。
- 完整 BGA 等級規則防呆。
- 3D 桌面、物理拖拉、自由擺放物件。
- 公開營運與陌生人配對。
- AI 玩家。

## 畫面架構

### 桌面主畫面

主畫面採用 BGA 式資訊配置：

- 上方狀態列：目前回合、階段、輪到誰、必須選擇的行動。
- 中央左側：主行動板。
- 中央右側：目前檢視的玩家農場板，預設顯示自己。
- 右側欄：玩家摘要、遊戲 log、設定。
- 下方固定區：自己的手牌。
- 卡片放大層：點擊或 hover 卡牌時顯示詳細卡片。

### 主行動板

每個行動格是一個可點擊區塊。

主行動板的名稱與排列必須盡量貼近桌面實體遊戲。行動格的位置不是單純視覺排版，而是規則資料的一部分；許多卡牌效果會指定特定行動格、行動類型或鄰近關係，所以每個行動格都需要穩定的 `actionSpaceId` 與固定座標。

行動格需要顯示：

- 行動名稱，例如森林、蘆葦、漁獲、職業、建造房間。
- 累積資源數量，例如木頭 4、蘆葦 2、食物 3。
- 是否已被佔用。
- 佔用玩家的家庭成員。
- 是否為目前玩家可選行動。

點擊行動格後：

- 顯示行動確認面板。
- 若有多種選項，讓玩家選擇。
- 確認後自動派出家庭成員。
- 自動拿取該格資源。
- 寫入 log。
- 換下一位玩家。

### 玩家摘要

右側每位玩家一張摘要卡。

顯示：

- 玩家名稱、顏色、座位。
- 木、磚、蘆葦、石、麥、菜、食物。
- 羊、豬、牛。
- 家庭成員數 / 可用家庭成員。
- 柵欄數、馬廄數。
- 房間材質與房間數。
- 已出職業數、已出次要發展卡數。
- 目前分數估算可晚一點做。

玩家摘要可點擊，切換中央右側顯示該玩家農場。

### 玩家農場板

農場板是 15 格固定網格。

每格可放：

- 房間。
- 田地。
- 牧場。
- 馬廄。
- 動物。
- 空地。

第一版操作方式：

- 不做任意拖拉。
- 透過行動面板選擇「放在哪一格」。
- 支援必要時手動修正格子狀態。

農場板上方顯示完整資源列，方便玩家手動加減。

### 手牌區

底部固定顯示自己的手牌。

功能：

- 職業 / 次要發展卡分組。
- 橫向排列。
- hover 或點擊放大。
- 搜尋卡名。
- 篩選牌組。
- 點卡牌可選：
  - 打出。
  - 丟棄。
  - 放大查看。
  - 加入備註。

### 已出牌區

每位玩家農場下方或旁邊顯示已出牌。

分區：

- 職業。
- 次要發展卡。
- 主要設施。

卡片操作：

- 點擊放大。
- 標記已發動。
- 放置計數標記。
- 手動加減資源。
- 寫入 log。

### 遊戲 log

log 是第一版核心功能，不可省略。

範例：

- `第 2 回合補料：森林 +3 木，蘆葦 +1，漁獲 +1 食物`
- `紅色 派家庭成員到森林，獲得木頭 +6`
- `藍色 打出職業：木匠`
- `紫色 發動卡片：市場攤位，獲得蔬菜 +1`
- `收成開始`
- `綠色 支付食物 -6`

log 需要支援：

- 自動寫入。
- 手動備註。
- 依回合分段。
- 之後可考慮匯出。

## 玩家流程

### 進房

1. 建立房間。
2. 分享房間連結。
3. 玩家輸入名稱。
4. 玩家選擇紅、綠、藍、紫顏色。
5. 房主選擇開局模式與輪抽模式。
6. 房主按開始。
7. 系統自動隨機分配座位。
8. 系統依座位產生玩家順序與起始玩家。

### 開局設定

房主開始遊戲前需要設定：

- 玩家人數，第一版以 4 人為主。
- 是否輪抽。
- 輪抽模式：同時輪抽或輪流輪抽。
- 牌組範圍。
- 每位玩家起始職業卡數量。
- 每位玩家起始次要發展卡數量。

### 輪抽流程

輪抽是第一版重要流程，因為你們固定玩家群會重視開局選牌體驗。

支援兩種模式：

- 同時輪抽：每位玩家同時選卡，所有人選完後一起傳牌。
- 輪流輪抽：依座位順序逐位選卡，適合需要明確追蹤選牌順序的局。

輪流輪抽傳牌方向：

- 職業卡：順時鐘傳。
- 次要發展卡：逆時鐘傳。

輪抽畫面需要顯示：

- 目前輪抽階段：職業或次要發展卡。
- 目前第幾手。
- 目前該誰選。
- 自己可選的卡。
- 已選卡區。
- 等待其他玩家提示。

輪抽完成後：

- 已選職業進入玩家職業手牌。
- 已選次要發展卡進入玩家次要發展卡手牌。
- 進入正式遊戲桌面。

### 回合流程

每回合包含：

1. 補料階段。
2. 工作階段。
3. 返家階段。
4. 收成階段，僅第 4、7、9、11、13、14 回合。

### 工作階段

1. 系統顯示目前玩家。
2. 可選行動格亮起。
3. 玩家點擊行動格。
4. 系統顯示確認面板。
5. 玩家確認。
6. 系統派出家庭成員、調整資源、寫入 log。
7. 換下一位玩家。

### 返家

條件：

- 所有玩家可用家庭成員都已派出。

動作：

- 按「返家」。
- 所有家庭成員回到各自農場。
- 行動格解除佔用。
- 進入下一回合或收成。

### 收成

第一版做提示與半自動：

- 提醒收成開始。
- 顯示每位玩家需支付食物。
- 玩家可按快捷鍵支付食物。
- 顯示繁殖提醒。
- 顯示田地收成提醒。
- 允許玩家手動調整結果。

## MVP 自動化範圍

第一版自動：

- 補料。
- 派家庭成員。
- 行動格佔用。
- 拿取累積資源。
- 玩家資源加減。
- 返家。
- 回合推進。
- 基本 log。
- 手牌打出到已出牌區。

第一版半自動：

- 打職業費用。
- 打次要發展卡費用。
- 建房 / 改建 / 柵欄 / 馬廄。
- 收成支付食物。
- 動物繁殖。
- 卡牌效果發動。

第一版手動：

- 特殊卡牌條件判斷。
- 複雜連鎖效果。
- 卡片之間的互相觸發。
- 例外規則裁定。

## 卡牌互動設計

### 卡片基本資料

沿用現有 `cards.json`。

卡牌需要欄位：

- `id`
- `name`
- `deck`
- `type`
- `cost`
- `requirement`
- `text`
- `points`
- `tags`
- `image`

### 手動發動面板

點擊已出卡後顯示：

- 發動。
- 放置標記。
- 移除標記。
- 加資源。
- 扣資源。
- 加分。
- 加備註。

快捷資源：

- 木。
- 磚。
- 蘆葦。
- 石。
- 麥。
- 菜。
- 食物。
- 羊。
- 豬。
- 牛。

每個動作都寫入 log。

## 資料結構草案

### Room

```js
{
  id: "room_abc",
  status: "lobby" | "playing" | "finished",
  round: 1,
  phase: "prepare" | "work" | "returnHome" | "harvest",
  currentPlayerId: "player_red",
  startingPlayerId: "player_red",
  playerOrder: ["player_red", "player_green", "player_blue", "player_purple"],
  createdAt: 0,
  updatedAt: 0
}
```

### Player

```js
{
  id: "player_red",
  roomId: "room_abc",
  name: "bboylu",
  color: "red",
  resources: {
    wood: 0,
    clay: 0,
    reed: 0,
    stone: 0,
    grain: 0,
    vegetable: 0,
    food: 3
  },
  animals: {
    sheep: 0,
    boar: 0,
    cattle: 0
  },
  workers: {
    total: 2,
    available: 2,
    guests: 0
  },
  fences: 15,
  stables: 4
}
```

### Action Space

```js
{
  id: "forest_3wood",
  name: "森林",
  type: "accumulation",
  accumulation: {
    wood: 3
  },
  tokens: {
    wood: 6
  },
  occupiedBy: null,
  enabledFromRound: 1
}
```

### Farm

```js
{
  playerId: "player_red",
  cells: [
    {
      index: 0,
      terrain: "room" | "field" | "pasture" | "empty",
      roomType: "wood" | "clay" | "stone" | null,
      stable: false,
      animals: {}
    }
  ]
}
```

### Card State

```js
{
  cardId: "A119",
  ownerId: "player_red",
  location: "hand" | "played" | "discarded" | "removed",
  playedRound: 3,
  counters: 0,
  notes: ""
}
```

### Log

```js
{
  id: "log_001",
  roomId: "room_abc",
  round: 2,
  playerId: "player_red",
  type: "take_action",
  message: "紅色 派家庭成員到森林，獲得木頭 +6",
  createdAt: 0
}
```

## 技術方向

先做單機原型，再做多人同步。

建議階段：

1. 單機狀態原型。
2. 本機互動 UI。
3. 接入現有卡牌資料。
4. Firebase 房間同步。
5. 多人操作防呆。
6. 規則補強。

多人同步建議使用 Firebase Firestore：

- `rooms/{roomId}`
- `rooms/{roomId}/players/{playerId}`
- `rooms/{roomId}/logs/{logId}`
- `rooms/{roomId}/cards/{cardStateId}`

需要避免的問題：

- 兩個玩家同時點同一個行動格。
- 非目前玩家操作。
- 重複補料。
- 重複返家。

這些之後可用 Firestore transaction 或後端 API 防呆。

## 開發里程碑

### Milestone 1：單機可玩骨架

目標：不用連線，能在一個頁面跑完基本行動。

內容：

- 顯示主行動板。
- 顯示 4 位玩家摘要。
- 顯示自己農場板。
- 點補料。
- 點行動格派人。
- 自動拿資源。
- 返家。
- log。

### Milestone 2：卡牌管理

目標：完成開局輪抽，並讓卡牌操作比 TTS 順。

內容：

- 開局發牌。
- 同時輪抽。
- 輪流輪抽。
- 職業卡順時鐘傳。
- 次要發展卡逆時鐘傳。
- 顯示手牌。
- 卡牌放大。
- 搜尋卡牌。
- 打出卡牌。
- 已出牌區。
- 手動發動面板。
- 卡片操作寫入 log。

### Milestone 3：房間與同步

目標：4 人可連進同一局。

內容：

- 建立房間。
- 加入房間。
- 選座位。
- 同步房間狀態。
- 同步玩家資源。
- 同步 log。

### Milestone 4：收成與農場操作

目標：能完整玩 14 回合。

內容：

- 田地收成提示。
- 餵食面板。
- 動物繁殖提示。
- 建房 / 改建 / 柵欄 / 馬廄操作。
- 分數估算初版。

### Milestone 5：實戰修正

目標：固定玩家群實測。

內容：

- 找出最常用卡牌效果。
- 補快捷鍵。
- 補常見防呆。
- 優化手機 / 平板版面。
- 優化 log 可讀性。

## 從 TTS 搬過來的功能對照

| TTS 功能 | 網頁版做法 | MVP |
| --- | --- | --- |
| 選顏色 | 房間座位 | 必做 |
| 補料按鈕 | 回合補料 | 必做 |
| 行動格累積資源 | 數字狀態 | 必做 |
| 拖家庭成員 | 點行動格自動派人 | 必做 |
| 拖資源到玩家區 | 資源數字自動加減 | 必做 |
| 玩家資源計數器 | 玩家摘要與資源列 | 必做 |
| 返家按鈕 | 返家階段 | 必做 |
| TTS 聊天 log | 右側遊戲 log | 必做 |
| 手牌攤在桌邊 | 底部手牌列 | 必做 |
| 卡牌拖到桌上 | 打出到已出牌區 | 必做 |
| 卡牌效果手動處理 | 發動面板 + log | 必做 |
| 3D 視角 | 不做，改 2D | 不做 |
| 物理物件 | 不做，改資料狀態 | 不做 |

## 風險與注意事項

### 授權風險

若只供固定玩家群私下使用，風險較低；若要公開營運，需要注意：

- 農家樂名稱。
- 官方圖像。
- 卡牌文字。
- 版面美術。
- 卡片插圖。

公開版應考慮改名、避免官方素材，或取得授權。

### 規則複雜度

農家樂難點不在主板，而在卡牌例外。

解法：

- 第一版只做規則骨架。
- 卡牌效果先手動。
- log 記清楚。
- 只逐步自動化高頻卡牌。

### 同步與防呆

多人同步最怕同時操作。

解法：

- 單一目前玩家。
- 行動確認時檢查回合與行動格狀態。
- 補料、返家需要防重複。
- 重要操作寫入 log。

## 下一步

建議下一步做 Milestone 1：

1. 建立一個新頁面，例如 `play.html`。
2. 建立單機 `game-state.js`。
3. 實作 4 位玩家初始狀態。
4. 實作主行動格資料。
5. 實作補料、派人、拿資源、返家、log。

Milestone 1 完成後，再把現有 `cards.json` 接進手牌與已出牌區。
## 2026-06-16 行動板修正

主行動板先以目前 TTS 桌面排版為準，而不是用抽象網頁格子重排。

- 左板：基礎行動板，包含 `2 根木頭`、`2 塊磚頭`、`擴建房舍`、`1 根木頭`、`拿 1 組蘆葦、1 顆石頭與 1 份食物`、`起始玩家`、`賣藝`、兩格 `打 1 張職業卡`、`拿 1 份麥子`、`犁 1 塊農田`、`臨時工`。
- 中央板：第一批回合行動與累積格，包含 `打 1 張主要或次要發展卡`、`建造柵欄`、`1 顆石頭`、`3 根木頭`、`播種 / 烤麵包`、`增加家庭成員`、`1 塊磚頭`、`1 隻羊`、`翻修房舍`、`1 組蘆葦`、`釣魚`。
- 右板：後期回合行動，包含 `拿 1 份蔬菜`、`1 顆石頭`、`增加家庭成員`、`翻修房舍 / 建造柵欄`、`1 隻野豬`、`1 頭牛`、`犁 1 塊農田 / 播種`。

每個行動格都要保留穩定 `actionSpaceId` 與 TTS 相對座標，讓之後卡牌可以指定特定格子、同類格子或回合格。
## 2026-06-16 正典時序實作

依 `D:\Dropbox\Lu_Agent\600_Project\烏嘎嘎桌遊\agricola-viewer\農家樂_正典時序_流程.md`，原型流程改用穩定 `phaseId`，不要只靠畫面文字判斷。

回合順序固定為：

1. `ROUND_START`：回合開始，翻開該回合卡。
2. `REPLENISH_PHASE`：準備階段，補累積行動格資源。
3. `WORK_PHASE_START`：工作階段開始。
4. `WORK_PHASE`：玩家依序派家庭成員。
5. `WORK_PHASE_END`：所有家庭成員派完，工作階段結束。這個時機早於收成，因此收益可用於本次收成。
6. `RETURN_HOME`：返家階段，收回家庭成員。
7. 收成回合才進 `HARVEST_START` → `HARVEST_FIELD` → `HARVEST_FEED` → `HARVEST_BREED` → `HARVEST_END`。
8. `ROUND_END`：收成之後才結算回合結束。卡牌若寫「回合結束時」，必須掛在此時機，不能誤放到 `WORK_PHASE_END`。

收成回合為第 4、7、9、11、13、14 回合。第 14 回合收成與 `ROUND_END` 後進入 `GAME_END`。

## 2026-06-16 農場操作 MVP

農莊初始狀態改為左下角直排兩間木屋，其餘 13 格為未使用地。這和 BGA / 實體版的初始圖版一致，也方便之後卡牌效果用格子 index 判斷。

目前已接上的農場操作：

- 農場查看：主農場區可以切換查看 4 位玩家農場；農場操作完成後會停留在剛操作的玩家農場，避免派完人切換到下一位後看不到變化。
- 犁田：確認行動後，玩家需要在農場選擇 1 格未使用地。
- 播種：確認行動後先選麥子或蔬菜，再選 1 塊空田。麥子在田上放 3 份，蔬菜在田上放 2 份。
- 擴建房舍：確認行動後，玩家需要選擇與既有房間相鄰的未使用地，並支付目前房屋材質的房間費用。
- 建造馬廄：在擴建房舍行動中可選擇建造馬廄，支付 2 木，放在未使用地或圈地上。
- 建造柵欄：MVP 先做單格圈地，選 1 格未使用地，支付 4 木與 4 根柵欄，容量 2 隻同種動物。
- 動物容量：房舍可養 1 隻寵物；單格圈地容量 2；圈地內有馬廄容量 4；單獨馬廄容量 1。取得動物時會自動放入農場，裝不下的退回供應區並寫入紀錄。
- 增加家庭成員：一般格需要有空房；「不需空房」格可直接增加家庭成員。
- 新生兒：同回合收成餵養時只需要 1 份食物，回合結束後轉為一般家庭成員。

卡牌顯示：

- 頁面會讀取 `cards.json`，依 `牌組/卡片ID` 對應 `images/cards/{牌組}/{卡片ID}.webp`。
- 手牌改採輪抽模擬器的呈現邏輯：職業 7 張與次要發展卡 7 張分區顯示，完整卡圖優先，牌名與 ID 簡潔列在下方。
- 開局輪抽改採輪抽模擬器式呈現：行動板作為背景並虛化，中央單一面板顯示目前玩家、已扣牌數、正在扣第幾包牌、玩家進度、卡片網格與確認選擇按鈕。
- 新增玩家備忘錄：上方工具列可開啟，每位玩家各自一份文字備忘，單機版先存於瀏覽器本機資料。
- 輪抽中央卡片改成可讀版：顯示真實卡圖、牌名、ID、費用/條件與效果文字，避免只看到小縮圖。
- 輪抽卡片補上右下角 `i` 資訊鈕：點擊只開卡片詳情，不會選牌；選牌需點卡片內的「選擇」並再按「確認選擇」。
- 新增旁觀者模式與聊天室原型：旁觀者可以觀看、切換農場、聊天與開備忘錄，但不能執行會改變遊戲狀態的操作。
- 第一版點擊手牌仍是直接打出卡牌；之後可再加「查看詳情 / 確認打出」兩段式操作。

仍待補齊：

- 完整自由形狀柵欄、跨多格圈地與手動調整動物位置。
- 烤麵包與主要設施的食物轉換。
- 動物收納空間與繁殖時的空間檢查。
- 發展卡的費用、先決條件與手動觸發效果。
