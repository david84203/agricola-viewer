const fs = require('fs'); const files = fs.readdirSync('.').filter(f => f.endsWith('.html')); let updated = 0; files.forEach(f => { let content = fs.readFileSync(f, 'utf8'); let newContent = content.replace(/<script src="([^"]+\.js)(?:\?v=[^"]+)?"><\/script>/g, '<script src="=20260608"></script>'); if (content !== newContent) { fs.writeFileSync(f, newContent); updated++; console.log('Updated ' + f); } }); console.log('Total files updated: ' + updated);
